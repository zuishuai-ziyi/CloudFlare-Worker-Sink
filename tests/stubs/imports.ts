// Stub for Nuxt's virtual `#imports` module. Tests that exercise code depending
// on Nuxt auto-imports replace this module with `vi.mock('#imports', ...)`; the
// empty export exists so Vitest can resolve the specifier everywhere else.
export {}
